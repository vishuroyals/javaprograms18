package basic;

public class EquailaterialTriangle {
	public static void main(String[] args) { // sqrt(3)/2 * a*a
		int a = 2;
		double area;
		area = Math.sqrt(3.0) / 2 * a * a;
		System.out.println(area);

	}
}
